% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TCGA_download.R
\name{tcga_download_clinic}
\alias{tcga_download_clinic}
\title{Download TCGA Clinical Supplement Data (BCR XML format)}
\usage{
tcga_download_clinic(
  project,
  manifest,
  download_dir = "GDCdata",
  manifest_only = FALSE
)
}
\arguments{
\item{project}{A \code{character} string specifying the TCGA project ID (e.g., \code{"TCGA-LGG"}).}

\item{manifest}{An optional \code{character} file path to save a manifest file.
If not provided, the manifest will be generated from the \code{GDCquery} result.}

\item{download_dir}{A \code{character} string specifying the directory where data
should be downloaded. Default is \code{"GDCdata"}.}

\item{manifest_only}{A \code{logical} value indicating whether to only create the
manifest file (\code{TRUE}) or also download the data (\code{FALSE}). Default is \code{FALSE}.}
}
\value{
Returns the internal query object invisibly. Nothing is printed unless assigned to a variable.
The function performs side effects: writing a manifest file and/or downloading data into the specified directory.
}
\description{
This function provides a convenient wrapper to download \strong{TCGA Clinical Supplement}
data in \strong{BCR XML} format using the \code{TCGAbiolinks} package. It supports downloading
either just the manifest file or the full dataset.
}
\examples{
\dontrun{
  tcga_download_clinic(
    project = "TCGA-LGG",
    manifest_only = TRUE
  )
}

}
